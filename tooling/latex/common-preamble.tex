% tooling/latex/common-preamble.tex
% Global CI-safe helpers injected into every build via latexmkrc.
% Code rendering is provided through minted only.

\providecommand{\safeincludegraphics}[2][]{%
  \IfFileExists{#2}{%
    \includegraphics[#1]{#2}%
  }{%
    % silent skip (non-fatal)
  }%
}



