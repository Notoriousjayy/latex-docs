\documentclass[11pt]{article}
\usepackage[letterpaper,margin=0.72in,top=0.82in,bottom=0.72in,headheight=22pt]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[table]{xcolor}
\usepackage{amsmath,amssymb,mathtools}
\usepackage{array,longtable,booktabs,tabularx}
\usepackage[inline]{enumitem}
\usepackage[most]{tcolorbox}
\usepackage{fancyhdr}
\usepackage{hyperref}

\definecolor{Primary}{HTML}{17324D}
\definecolor{Accent}{HTML}{2C7A7B}
\definecolor{CueTint}{HTML}{EAF1F5}
\definecolor{NoteTint}{HTML}{F8FAFB}
\definecolor{Rule}{HTML}{B8C6CF}
\hypersetup{hidelinks,pdftitle={Chapter 19: Models of Genome-Level Mutations},pdfsubject={Cornell notes}}
\setlist[itemize]{leftmargin=1.25em,itemsep=1pt,topsep=1.5pt,parsep=0pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{3pt}
\renewcommand{\arraystretch}{1.16}
\emergencystretch=2em

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\sffamily\small\color{Primary} Chapter 19}
\fancyhead[R]{\sffamily\small\color{Primary} Genome rearrangements}
\fancyfoot[C]{\sffamily\small\color{Primary}\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{Accent}\leaders\hrule height \headrulewidth\hfill}}

\newcolumntype{C}{>{\raggedright\arraybackslash}p{0.275\textwidth}}
\newcolumntype{N}{>{\raggedright\arraybackslash}p{0.655\textwidth}}
\newcommand{\cnrow}[2]{%
  \cellcolor{CueTint}\textbf{\sffamily\color{Primary}#1} &
  \cellcolor{NoteTint}#2 \\[4pt]\arrayrulecolor{Rule}\hline}

\begin{document}

\begin{tcolorbox}[
  enhanced,breakable,colback=Primary!4,colframe=Primary,boxrule=0.9pt,
  arc=2mm,left=3mm,right=3mm,top=2.5mm,bottom=2.5mm]
{\sffamily\color{Accent}\bfseries CHAPTER 19}\\[-1pt]
{\sffamily\LARGE\bfseries\color{Primary} Models of Genome-Level Mutations}

\vspace{2pt}
\textbf{Purpose.} Model large-scale evolutionary change as reversals of blocks in unsigned and signed permutations and reason about rearrangement distance.

\textbf{Learning objectives}
\begin{itemize}
  \item Encode gene order as a permutation.
  \item Define unsigned and signed reversals.
  \item Use adjacencies, breakpoints, strips, and bounds to guide sorting.
  \item Explain why orientation information changes the algorithmic problem.
\end{itemize}

\textbf{Prerequisites.} Permutations, greedy algorithms, graph cycles, approximation, and genome orientation.
\end{tcolorbox}

\begin{longtable}{@{}C!{\color{Accent}\vrule width 0.8pt}N@{}}
\rowcolor{Primary}
\color{white}\sffamily\bfseries Cue / question &
\color{white}\sffamily\bfseries Notes \\ \hline
\endfirsthead
\rowcolor{Primary}
\color{white}\sffamily\bfseries Cue / question &
\color{white}\sffamily\bfseries Notes (continued) \\ \hline
\endhead
\cnrow{\S19.1: Why use a genome-level model?}{Point edits cannot describe inversions or other events that move long blocks. Abstract a chromosome by the order of unique markers or syntenic blocks. The model ignores nucleotide detail to focus on large-scale organization.}
\cnrow{How is a genome represented?}{Choose one genome as the identity order $(1,2,\ldots,n)$. Represent the other as a permutation of the same markers. Add sentinel elements at both ends when defining adjacencies so incorrect boundary placement is counted consistently.}
\cnrow{What is rearrangement distance?}{Given an allowed operation set, the distance is the minimum number or total cost of operations transforming one genome representation into the other. It is model dependent: allowing reversals, transpositions, duplications, or unequal gene content yields different problems.}
\cnrow{\S19.2: What is an unsigned inversion?}{Choose positions $i\le j$ and reverse the order of the block $\pi_i,\ldots,\pi_j$. Marker orientation is ignored. A reversal can repair boundary adjacencies but may also disrupt correct internal structure, so a purely local greedy choice needs analysis.}
\cnrow{What are breakpoints and strips?}{An adjacency is correct when neighboring values are consecutive in the target order under the chosen unsigned convention. A breakpoint is an incorrect adjacency. Maximal runs without internal breakpoints are strips. A reversal changes only the adjacencies at its two boundaries, giving lower bounds on the number of required reversals.}
\cnrow{How does a breakpoint-guided method work?}{Select a reversal that removes breakpoints or creates longer target-consistent strips, repeat, and finish when no breakpoints remain. Correctness requires that zero breakpoints implies the target under the sentinel convention. Approximation analysis compares progress per reversal with the maximum progress any reversal can make.}
\cnrow{Why are lower bounds useful?}{If one reversal can remove at most a fixed number of breakpoints, the current breakpoint count divided by that number bounds the optimum from below. Comparing an algorithm's operation count with this bound yields an approximation ratio, but only for the exact breakpoint definition and model used.}
\cnrow{What makes unsigned sorting difficult?}{Ignoring orientation loses information about how a reversed block should face. Local adjacency choices can conflict, and exact unsigned reversal distance is computationally hard in the general model. Practical solutions use approximation, branch-and-bound, or domain constraints.}
\cnrow{\S19.3: What is a signed inversion?}{Each marker carries an orientation sign. Reversing a block reverses its order and flips every sign. The target is usually $(+1,+2,\ldots,+n)$. Orientation makes adjacencies more informative and permits stronger graph-based structure.}
\cnrow{How do signed algorithms use graph cycles?}{Split signed markers into oriented extremities and connect observed adjacencies with target adjacencies in a breakpoint graph. Alternating cycles measure preserved structure; a reversal rewires boundary edges and changes the cycle decomposition. Exact distance formulas may require additional obstruction terms beyond a simple breakpoint count.}
\cnrow{What implementation issues matter?}{Normalize marker names, verify one-to-one content before forming a permutation, define circular versus linear chromosomes, handle multiple chromosomes explicitly, and apply sign flips to the entire reversed block. Duplicated or missing markers require a richer model.}
\cnrow{\S19.4: What should practice emphasize?}{Apply signed and unsigned reversals by hand, compute sentinels and breakpoints, construct a small breakpoint graph, and distinguish a lower bound, an approximation guarantee, and an exact distance result.}
\end{longtable}
\enlargethispage{1\baselineskip}

\begin{tcolorbox}[enhanced,breakable,title={Chapter synthesis},
  colback=Accent!5,colframe=Accent,fonttitle=\small\sffamily\bfseries,fontupper=\footnotesize,
  boxsep=0.6mm,left=1.5mm,right=1.5mm,top=1mm,bottom=1mm,before skip=3pt,after skip=3pt]
Genome rearrangement models compress each conserved block to a permutation element and measure evolution by block operations. Unsigned reversals change order only and support breakpoint-guided bounds but lose orientation information. Signed reversals also flip orientation and admit a richer breakpoint-graph analysis. Every distance claim is meaningful only relative to the chosen markers and operation set.
\end{tcolorbox}

\begin{tcolorbox}[enhanced,breakable,title={Key takeaways},
  colback=white,colframe=Primary,fonttitle=\small\sffamily\bfseries,fontupper=\footnotesize,
  boxsep=0.6mm,left=1.5mm,right=1.5mm,top=1mm,bottom=1mm,before skip=3pt,after skip=3pt]
\begin{itemize}
  \item Rearrangement distance is defined by an operation model.
  \item A reversal changes only two external block boundaries.
  \item Breakpoint counts provide lower bounds, not automatically exact distances.
  \item Unsigned reversal distance is harder because orientation is absent.
  \item A signed reversal reverses order and flips signs.
\end{itemize}
\end{tcolorbox}

\begin{tcolorbox}[enhanced,breakable,title={Algorithm selection / when to use this},
  colback=Primary!3,colframe=Primary,fonttitle=\small\sffamily\bfseries,fontupper=\footnotesize,
  boxsep=0.6mm,left=1.5mm,right=1.5mm,top=1mm,bottom=1mm,before skip=3pt,after skip=3pt]
\begin{itemize}
  \item Use unsigned permutations when marker orientation is unavailable or intentionally ignored.
  \item Use signed permutations when block orientation is known.
  \item Use breakpoint bounds to evaluate greedy or approximate solutions.
  \item Use graph-based signed methods when exact orientation-aware distance is required.
\end{itemize}
\end{tcolorbox}

\begin{tcolorbox}[enhanced,breakable,title={Self-test questions},
  colback=white,colframe=Accent,fonttitle=\small\sffamily\bfseries,fontupper=\footnotesize,
  boxsep=0.6mm,left=1.5mm,right=1.5mm,top=1mm,bottom=1mm,before skip=3pt,after skip=3pt]
\begin{itemize}
  \item How is a genome pair normalized to an identity permutation?
  \item Why does the breakpoint count provide a lower bound?
  \item What additional action occurs in a signed reversal?
\end{itemize}
\end{tcolorbox}

{\footnotesize
\textbf{Terms and notation to remember.} permutation, sentinel, breakpoint, strip, signed reversal, extremity, breakpoint graph.

\textbf{Connections.} Rearrangement distance extends comparison from symbol edits to chromosome-scale operations.
}

\end{document}
