@startuml
skinparam nodesep 25
!include ../../../../../../tooling/styles/plantuml/structural/class-diagram-style.iuml

skinparam ranksep 35

title **Collections Framework Hierarchy**\nA classic example of layered generalization

interface "Iterable" as iter $deep_alt {
  + iterator()
}

interface "Collection" as coll $deep_alt {
  + add(e)
  + remove(e)
  + size()
}

interface "List" as list $fill_alt {
  + get(index)
  + set(index, e)
}

interface "Set" as set $fill_alt {
  .. no duplicates ..
}

interface "Queue" as queue $fill_alt {
  + offer(e)
  + poll()
  + peek()
}

abstract class "AbstractCollection" as abscoll $fill_ok {
  + isEmpty()
  + toArray()
}

abstract class "AbstractList" as abslist $fill_ok {
  + indexOf(e)
  + subList()
}

class "ArrayList" as alist $fill_primary {
  - elementData[]
  + get(index)
}

class "LinkedList" as llist $fill_primary {
  - first : Node
  + addFirst(e)
}

class "HashSet" as hset $fill_primary {
  - map : HashMap
  + add(e)
}

class "PriorityQueue" as pq $fill_primary {
  - queue[]
  + poll()
}

iter <|-- coll
coll <|-- list
coll <|-- set
coll <|-- queue
coll <|.. abscoll
abscoll <|-- abslist
list <|.. abslist
abslist <|-- alist
list <|.. llist
queue <|.. llist
set <|.. hset
queue <|.. pq

legend bottom
  **Purple** = Interfaces (contracts at each level)
  **Green** = Abstract classes (partial reusable implementations)
  **Blue** = Concrete classes (complete, instantiable implementations)
end legend

@enduml
