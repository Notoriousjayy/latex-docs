@startuml

title **Module Types in Generalization**\nRoles modules play in inheritance hierarchies

!include ../../../../../../tooling/styles/plantuml/structural/class-diagram-style.iuml

package "Abstract Modules" $fill_caution {
  abstract class "AbstractModule" as absmod $deep_caution {
    + concreteMethod()
    {abstract} + abstractMethod()
  }
}

package "Interface Modules" $fill_alt {
  interface "IContract" as iface $deep_alt {
    + operationA()
    + operationB()
  }
}

package "Concrete Modules" $fill_ok {
  class "ConcreteModule" as conmod $deep_ok {
    + concreteMethod()
    + abstractMethod()
    + ownMethod()
  }
}

package "Mixin / Trait Modules" $deep_primary {
  class "<<mixin>> Loggable" as mixin $fill_primary {
    + log(message)
  }
  class "<<trait>> Serializable" as trait $fill_primary {
    + serialize()
    + deserialize()
  }
}

absmod <|-- conmod
iface <|.. conmod

note bottom of absmod
  Cannot be instantiated.
  Partial implementation;
  children must complete it.
end note

note bottom of iface
  Pure contract: no implementation.
  Multiple unrelated modules
  can implement the same interface.
end note

note bottom of conmod
  Fully implemented; instantiable.
  May inherit from abstract module
  or implement interfaces.
end note

note bottom of mixin
  Reusable functionality
  composed into classes.
  Traits add conflict resolution.
end note

@enduml
