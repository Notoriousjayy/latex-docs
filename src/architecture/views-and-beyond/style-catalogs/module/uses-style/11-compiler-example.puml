@startuml

skinparam backgroundColor white
skinparam defaultFontSize 13
skinparam ArrowColor #2C3E50
skinparam ArrowThickness 2
skinparam RectangleBorderColor #2C3E50
skinparam RectangleFontSize 13

title Example: Compiler Pipeline Uses Structure

rectangle "**Code Generator**" as CG #FFE0B2
rectangle "**Optimizer**" as OPT #FFF9C4
rectangle "**Semantic Analyzer**" as SA #C8E6C9
rectangle "**Parser**" as P #BBDEFB
rectangle "**Lexer**" as L #E3F2FD

CG -down-> OPT : <<uses>>\noptimized IR
OPT -down-> SA : <<uses>>\nannotated AST
SA -down-> P : <<uses>>\nabstract syntax tree
P -down-> L : <<uses>>\ntoken stream

note right of L
  **Foundation**
  No dependencies;
  develop first
end note

note right of CG
  **Top-level**
  Depends on all below;
  develop last
end note

note as N1
  **Pipeline Uses Pattern**
  Each module uses exactly
  one predecessor. Linear
  chain enables straightforward
  development sequencing.
end note
@enduml
