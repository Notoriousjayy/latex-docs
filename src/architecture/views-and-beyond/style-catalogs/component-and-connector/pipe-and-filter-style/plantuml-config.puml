' Rendering-only configuration for this directory (applied through -config).
' Visual rules live in the shared style modules; the filter-role colours that
' used to be defined here moved to pipe-and-filter-roles.iuml so they draw
' from the house tokens. This file intentionally sets nothing beyond the
' repository default so it can be deleted once no override is needed.
skinparam dpi 150
