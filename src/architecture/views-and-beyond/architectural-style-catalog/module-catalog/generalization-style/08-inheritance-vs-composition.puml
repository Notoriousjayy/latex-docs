@startuml
skinparam backgroundColor #FFFFFF
skinparam defaultFontSize 12
skinparam shadowing false
skinparam dpi 150
skinparam nodesep 50
skinparam ranksep 40

skinparam class {
  BorderColor #2C3E50
  ArrowColor #2C3E50
  FontColor #2C3E50
  FontSize 13
  AttributeFontSize 11
}

skinparam package {
  BorderColor #888888
  FontSize 13
}

skinparam note {
  BackgroundColor #FFFFF0
  BorderColor #999999
  FontSize 11
}

title **Inheritance vs. Composition**\nTwo approaches to reusing behavior

package "Inheritance (is-a)" #FDEBD0 {
  class "Logger" as log1 #FFF3E0 {
    + log(msg)
    + setLevel(lvl)
  }
  class "FileLogger" as flog #E8F4FD {
    + log(msg)
    - filePath
  }
  class "DatabaseLogger" as dlog #E8F4FD {
    + log(msg)
    - connection
  }
  log1 <|-- flog
  log1 <|-- dlog
}

package "Composition (has-a)" #D5E8D4 {
  interface "LogWriter" as lw #F3E5F5 {
    + write(msg)
  }
  class "FileWriter" as fw #E8F5E9 {
    + write(msg)
  }
  class "DatabaseWriter" as dw #E8F5E9 {
    + write(msg)
  }
  class "Logger " as log2 #E8F4FD {
    - writer : LogWriter
    + log(msg)
    + setLevel(lvl)
  }
  lw <|.. fw
  lw <|.. dw
  log2 o--> lw : delegates
}

note bottom of log1
  **Tight coupling** to parent.
  FileLogger //is-a// Logger.
  Changes to Logger may break children.
end note

note bottom of log2
  **Loose coupling** via interface.
  Logger //has-a// writer.
  Writers are swappable at runtime.
end note

legend bottom
  Prefer **composition** for behavior reuse and flexibility.
  Use **inheritance** for true //is-a// relationships and polymorphism.
end legend

@enduml
