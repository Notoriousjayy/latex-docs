@startuml
skinparam backgroundColor #FEFEFE
skinparam defaultFontSize 11
skinparam shadowing false
skinparam rectangleRoundCorner 8
skinparam rectangleFontSize 12
skinparam rectangleFontStyle bold
skinparam packageFontSize 13
skinparam packageFontStyle bold
skinparam nodeRoundCorner 8
skinparam nodeFontSize 12
skinparam nodeFontStyle bold

title Layers vs. Tiers\nLogical code organization vs. physical deployment

package "Layers (Logical -- Design Time)" as LAYERS #F0F4FF {
  rectangle "**Presentation**" as LP #B3D9FF
  rectangle "**Business Logic**" as LB #CCE5CC
  rectangle "**Data Access**" as LD #FFE0B2

  LP -[#3366AA]-> LB
  LB -[#3366AA]-> LD
}

package "Tiers (Physical -- Deployment Time)" as TIERS #FFF8F0 {
  node "**Client Tier**\nBrowser / Mobile" as TC #B3D9FF
  node "**Application Tier**\nServer" as TA #CCE5CC
  node "**Database Tier**\nDB Server" as TD #FFE0B2

  TC -[#CC6633]-> TA : HTTP / API
  TA -[#CC6633]-> TD : SQL / ORM
}

LAYERS -[hidden]right-> TIERS

note bottom of LAYERS
  **Layers** constrain code
  dependencies (compile-time).
  Same layers can deploy
  as 1, 2, or 3 tiers.
end note

note bottom of TIERS
  **Tiers** constrain runtime
  distribution (deploy-time).
  Communication crosses
  network boundaries.
end note
@enduml
