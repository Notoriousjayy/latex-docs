\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage[hidelinks]{hyperref}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\begin{document}

\title{Variation Points}
\author{}
\date{}
\maketitle

\begin{table}[h!]
  \centering
  \small
  \begin{tabular}{L{2.5cm} L{2.7cm} L{4.2cm} L{5.3cm} L{1.8cm}}
    \toprule
    \textbf{Variation Point} &
    \textbf{Affected Element or Relation} &
    \textbf{Variants} &
    \textbf{Condition} &
    \textbf{Binding Time} \\
    \midrule
    \textbf{VP1}: Host name of the SMTP server used by the system to send e-mail messages
      & \textit{emailer}
      & Any valid host name.
      & Whenever the SMTP host changes. Also used to switch between development and test environments.
      & Load time \\
    \midrule
    \textbf{VP2}: External storage and content delivery services in use
      & \textit{filemanager}
      & List of Web services; shall differentiate whether the service is available in the development, test, and/or production environments.
      & At least one storage service must be configured. A submodule that handles communication with a given new service must be available prior to enabling a service.
      & Load time \\
    \midrule
    \textbf{VP3}: Access keys to storage and content delivery Web services
      & \textit{filemanager}
      & For each storage and content delivery service enabled (such as Scribd, S3, YouTube), there will be a set of access parameters (such as URL, user ID, password).
      & Only used if the corresponding service is enabled in VP2.
      & Runtime \\
    \midrule
    \textbf{VP4}: Performance-monitoring switch
      & \textit{util}; \textit{logging}; \textit{aspects}
      & True if the response time of requests needs to be monitored and recorded; false otherwise. Must be configurable separately for development, test, and production environments.
      & Must be set to true when the SLA is in effect or for debugging purposes. If false, the corresponding submodules and artifacts may be excluded from the build.
      & Build time \\
    \bottomrule
  \end{tabular}
  \caption{Variation points, affected elements, conditions, and binding times.}
\end{table}

\end{document}

